## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `pnpm install`
2. Set the `GOOGLE_CLIENT_ID`, `SPREADSHEET_ID`, `GOOGLE_API_KEY`, in [.env.local](.env.local)
3. Run the app:
   `pnpm run dev`
